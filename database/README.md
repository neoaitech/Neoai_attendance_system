# VisionAttend Database Directory

This directory houses the primary SQLite storage database, automated migration scripts, and backup archives.

- `attendance.db`: Primary production SQLite database containing student profiles, courses, faculty records, and attendance sessions.
- `backups/`: Timestamped SQLite backup archives created via admin panel or automated maintenance scripts.
